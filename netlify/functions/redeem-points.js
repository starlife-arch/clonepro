import { getDb, admin, json, cents } from './_lib/battle-royale.js';
export default async (req) => { try { const { userId, points } = await req.json(); const pts=Number(points||0); if(!userId||pts<1000||pts%1000!==0) return json({success:false,error:'Redeem points in multiples of 1000'},400); const usd=cents(pts/1000); const db=getDb(); await db.runTransaction(async t=>{ const ur=db.collection('users').doc(userId), pr=db.collection('gamePools').doc('pointsPool'); const [us,ps]=await Promise.all([t.get(ur),t.get(pr)]); if(!us.exists) throw new Error('User not found'); const u=us.data()||{}, p=ps.data()||{}; if(p.enabled===false||Number(p.balance||0)<usd) throw new Error('Points redemption temporarily unavailable. Please try again later.'); if(Number(u.points||0)<pts) throw new Error('Insufficient points'); t.update(ur,{points:admin.firestore.FieldValue.increment(-pts),balance:admin.firestore.FieldValue.increment(usd)}); t.set(pr,{balance:admin.firestore.FieldValue.increment(-usd),totalRedeemedAllTime:admin.firestore.FieldValue.increment(usd)},{merge:true}); t.set(ur.collection('pointsHistory').doc(),{points:-pts,desc:`Points redeemed — ${pts} pts → $${usd.toFixed(2)}`,createdAt:admin.firestore.FieldValue.serverTimestamp()}); t.set(db.collection('activity').doc(),{uid:userId,desc:`🏅 Points redeemed — ${pts} pts → $${usd.toFixed(2)}`,amt:usd,icon:'🏅',type:'points_redeem',createdAt:admin.firestore.FieldValue.serverTimestamp()}); }); return json({success:true,usdCredited:usd}); } catch(e){ return json({success:false,error:e.message||'Redeem failed'},500); } };
