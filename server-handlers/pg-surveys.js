import{query}from './_lib/postgres.js';import{requireUser}from './_lib/pg-auth.js';export async function surveys(req,res){try{res.json((await query("SELECT * FROM surveys WHERE status='active' ORDER BY created_at DESC")).rows)}catch(e){res.status(500).json({error:'Server error'})}}export async function respondSurvey(req,res){const{uid,surveyId,answers}=req.body||{};try{if(!await requireUser(req,res,uid))return;await query('INSERT INTO survey_responses (survey_id,user_id,answers) VALUES ($1,$2,$3)',[surveyId,uid,JSON.stringify(answers)]);res.status(201).json({success:true})}catch(e){res.status(500).json({error:'Server error'})}}
