import { query } from './_lib/postgres.js';
export default async function createUser(req,res){const {uid,name,email,phone,referredBy,role='user',status='active'}=req.body||{};if(!uid||!email)return res.status(400).json({error:'uid and email are required'});try{await query('INSERT INTO users (id,name,email,phone,referred_by,role,status) VALUES ($1,$2,$3,$4,$5,$6,$7) ON CONFLICT (id) DO UPDATE SET name=$2,email=$3',[uid,name||null,email,phone||null,referredBy||null,role,status]);await query('INSERT INTO wallets (user_id) VALUES ($1) ON CONFLICT (user_id) DO NOTHING',[uid]);return res.status(201).json({success:true});}catch(error){console.error('[pg] user create failed',error);return res.status(500).json({error:'Server error'});}}
