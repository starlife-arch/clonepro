import {query}from './_lib/postgres.js';import{requireUser}from './_lib/pg-auth.js';
export async function getKyc(req,res){try{if(!await requireUser(req,res))return;const{rows}=await query('SELECT * FROM kyc WHERE user_id=$1 ORDER BY submitted_at DESC LIMIT 1',[req.params.uid]);res.json(rows[0]||null)}catch(e){console.error('[pg] kyc',e);res.status(500).json({error:'Server error'})}}
export async function submitKyc(req,res){const{uid,documents}=req.body||{};try{if(!await requireUser(req,res,uid))return;const{rows:[row]}=await query("INSERT INTO kyc (id,user_id,documents,status) VALUES ($1,$2,$3,'pending') RETURNING *",[`kyc_${Date.now()}_${uid.slice(0,6)}`,uid,JSON.stringify(documents||{})]);res.status(201).json({success:true,kyc:row})}catch(e){console.error('[pg] kyc submit',e);res.status(500).json({error:'Server error'})}}
