import{query}from './_lib/postgres.js';import{requireUser}from './_lib/pg-auth.js';export async function predictions(req,res){try{res.json((await query("SELECT * FROM prediction_events WHERE status='open' ORDER BY created_at DESC")).rows)}catch(e){res.status(500).json({error:'Server error'})}}export async function predictionBet(req,res){const{uid,eventId,option,amount}=req.body||{};try{if(!await requireUser(req,res,uid))return;const{rows:[bet]}=await query('INSERT INTO prediction_bets (id,event_id,user_id,option,amount) VALUES ($1,$2,$3,$4,$5) RETURNING *',[`prediction_${Date.now()}_${uid.slice(0,6)}`,eventId,uid,option,amount]);res.status(201).json({success:true,bet})}catch(e){res.status(500).json({error:'Server error'})}}
