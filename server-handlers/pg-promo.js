import{query}from './_lib/postgres.js';import{requireUser}from './_lib/pg-auth.js';export default async function applyPromo(req,res){const{uid,code}=req.body||{};try{if(!await requireUser(req,res,uid))return;const{rows}=await query("UPDATE promo_codes SET uses=uses+1 WHERE code=$1 AND uses<max_uses AND (expires_at IS NULL OR expires_at>NOW()) RETURNING *",[code]);if(!rows[0])return res.status(400).json({error:'Invalid or expired promo code'});res.json({success:true,promo:rows[0]})}catch(e){res.status(500).json({error:'Server error'})}}
