import { query } from './_lib/postgres.js';
import { requireAdmin } from './_lib/pg-auth.js';
export default async function pgAdminUsers(req, res) { try { if (!await requireAdmin(req, res)) return; const { rows } = await query(`SELECT u.id, u.name, u.email, u.phone, u.role, u.status, u.kyc_status, COALESCE(w.balance, 0) AS balance, COALESCE(w.game_balance, 0) AS game_balance, COALESCE(w.held_balance, 0) AS held_balance, COALESCE(w.total_deposited, 0) AS total_deposited, COALESCE(w.total_withdrawn, 0) AS total_withdrawn, COALESCE(w.total_earned, 0) AS total_earned FROM users u LEFT JOIN wallets w ON w.user_id = u.id ORDER BY u.created_at DESC`); return res.json(rows); } catch (error) { console.error('[pg] admin users failed', error); return res.status(500).json({ error: 'Server error' }); } }
