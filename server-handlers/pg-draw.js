import{query}from './_lib/postgres.js';import{requireUser}from './_lib/pg-auth.js';export async function drawTickets(req,res){try{if(!await requireUser(req,res))return;res.json((await query('SELECT * FROM draw_tickets WHERE user_id=$1 ORDER BY created_at DESC',[req.params.uid])).rows)}catch(e){res.status(500).json({error:'Server error'})}}export async function buyDraw(req,res){const{uid,drawId,quantity=1}=req.body||{};try{if(!await requireUser(req,res,uid))return;const{rows:[ticket]}=await query('INSERT INTO draw_tickets (id,user_id,draw_id,quantity) VALUES ($1,$2,$3,$4) RETURNING *',[`draw_${Date.now()}_${uid.slice(0,6)}`,uid,drawId,quantity]);res.status(201).json({success:true,ticket})}catch(e){res.status(500).json({error:'Server error'})}}
